name: JPStock Build APK

on:
  push:
    branches: [main]
    paths:
      - 'jp-stock/android/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Compute build version
        id: ver
        run: echo "code=$(git rev-list --count HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build debug APK
        working-directory: jp-stock/android
        env:
          VERSION_CODE: ${{ steps.ver.outputs.code }}
          VERSION_NAME: "1.0.${{ steps.ver.outputs.code }}"
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: jpstock-apk
          path: jp-stock/android/app/build/outputs/apk/debug/app-debug.apk
